# Placeholder FPGA constraints for future emulation.
# This does NOT implement real 3D stacked memory.
# Use only for FPGA-side prototyping of the RTL behavioral model.

# Example clock placeholder:
# create_clock -period 10.000 [get_ports clk]

# Add board-specific pin constraints only after selecting the actual board.
